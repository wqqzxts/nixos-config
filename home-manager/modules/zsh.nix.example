{ config, ... }: {
  programs.zsh = {
    enable = true;
    enableCompletion = true;
    autosuggestion.enable = true;
    syntaxHighlighting.enable = true;

    shellAliases = {
      nu = "nix flake update --flake path:.";
      ns = "sudo nixos-rebuild switch --flake path:.";
      hs = "home-manager switch --flake path:.";
      gc = "nix-collect-garbage -d";

      v = "nvim";
      y = "yazi";

      t = "tmux";
      ta = "tmux new -As ";
      td = "tmux detach";

      gs = "git status";
      ga = "git add";
      gc = "git commit";
      gp = "git push";

      ".." = "cd ..";
      "cl" = "clear";

      ff = "fastfetch";
      nf = "nitch";

      ewwbar = "eww open ewwbar && eww open window-power && eww open window-clock && eww open window-weather && eww open window-cava && eww open window-language && eww open window-status && eww open window-battery";
    };

    history.size = 10000;
    history.path = "${config.xdg.dataHome}/zsh/history";

    initContent = ''
      # u can uncomment that but i prefer to use tmux without autorun
      # if [ -z "$TMUX" ] && [ -n "$DISPLAY" ]; then
      #   tmux attach-session -t default || tmux new-session -s default
      # fi
    '';

    # maybe cringe
    profileExtra = ''
      if [[ "$(tty)" = "/dev/tty1" ]]; then
        exec niri-session -l
      fi
    '';
  };
}
